/** Vue `<Transition>` / `<TransitionGroup>` name for rise + fade overlays. */
export const UI_RISE_TRANSITION = 'ui-rise' as const

/** Keep JS timing (e.g. leave grace) aligned with CSS `--ui-rise-duration`. */
export const UI_RISE_DURATION_MS = 220
